# S3 Storage Configuration for caching
# Optional: If not configured, will use in-memory storage (development mode)
# Configure all fields below to enable S3 storage (recommended for production)
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
